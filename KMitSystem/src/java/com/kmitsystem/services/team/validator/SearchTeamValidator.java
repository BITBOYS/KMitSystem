package com.kmitsystem.services.team.validator;

import com.kmitsystem.services.team.input.SearchTeamInput;

/**
 * @author Maik
 */
public class SearchTeamValidator {
    
    public boolean validate(SearchTeamInput input) {
        
        // check if the 
        
        // check if the user exists, if the user searches with it
                    
        // check if the tournament exists, if the user searches with it
        
        
        return true;
    }
    
}
